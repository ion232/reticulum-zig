const std = @import("std");

pub fn main() !void {
    // Setup node1 and node2.
    // Make a random incoming endpoint on node1 and node2.
    // Use these to register the corresponding outgoing endpoints on node1 and node2.
    // Construct a 'single' packet destined to node2 from node1 and vice versa.
    // Send this packet from node1.
}
