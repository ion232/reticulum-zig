# Overview

- Contains build setups for MCUs.
- Right now this is just a proof of concept demonstrating the following
  - Microzig can build images for common hardware like the rpi pico.
  - reticulum-zig will build and run on these targets.
  - zig build pico
